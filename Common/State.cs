namespace LogicGates.Common
{
    /// <summary>
    /// Enum used to define Connection states
    /// </summary>
    public enum State
    {
        HighImpedance = -1,
        Low = 0,
        High = 1
    }
}
