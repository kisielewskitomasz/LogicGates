namespace LogicGates.Common
{
    /// <summary>
    /// Enum used to define Mouse state
    /// </summary>
    public enum MS
    {
        Null = -1,
        Idle = 0,
        Select = 1,
        SelectPin = 2,
        Move = 3,
        Connect = 4

    }
}
